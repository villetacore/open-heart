# Changelog

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/); версии — семантические,
пока проект в 0.x (API и форматы могут меняться).

## [Unreleased]

### Added
- **Элитные аффиксы** (`affixes.json`, §7 плана): элита = базовый враг +
  1–2 аффикса — комбинаторный рост числа противников без новых ассетов.
  В core: Быстрый, Бронированный (сложнее прервать), Вампирический (лечится
  от урона игроку), Взрывной (посмертный взрыв), Свирепый. Элиты спавнятся
  в данже (шанс растёт с глубиной, настройки в dungeon.json), подсвечены
  тинтом, дают больше XP; в прицеле — ⭐ и имя с префиксом («Быстрый grunt»).
  Категория редактора «Аффиксы» + поля элит в настройках данжа.
- **Способности врагов** (`abilities.json`, §7 плана) — компонуются на врагах
  полем `abilities`, кастуются по кулдауну при прямой видимости, перед эффектом
  **телеграф** (враг стоит и подсвечивается цветом — окно на уворот):
  - `projectile_burst` — **вражеские снаряды** (веер в игрока; впервые атака,
    от которой можно увернуться; стены и «свои» гасят снаряд);
  - `charge` — рывок с контакт-уроном; `summon` — призыв миньонов (наследуют
    силу кастера); `heal_pulse` — лечение союзников вокруг.
  В core: огненный веер у культистов, void-болт у фантома/тени/жнеца, рывок
  у брута/варлорда, призыв у варлорда, лечение у фантома.
- **Pain/stagger** — `pain_chance` у врага: шанс при уроне прервать его на 0.4 с
  (сбрасывает телеграф и рывок — контр-игра против кастеров). Значения
  расставлены всем 14 врагам core.
- Категория редактора «Способности» (+поля abilities/pain_chance у врагов);
  тесты пресетов проверяют виды способностей и ссылки (враг→способность,
  summon→миньон).
- **ИИ врагов v2**:
  - **A\*-навигация в данже** (`rust/src/nav.rs`): враги преследуют игрока по
    сетке проходимости генератора и обходят стены (в мире — по-прежнему напрямик);
  - **агро по зрению** — врага нельзя «активировать» сквозь стену: нужен радиус
    И прямая видимость (рейкаст троттлится);
  - **слух**: выстрел будит врагов в радиусе 18 м (мили — 8 м), взрыв — 22 м;
  - **поведение из данных** — `behavior` в enemies.json: `melee` (в контакт) |
    `ranged` (держит дистанцию, пятится, если игрок налез); снайпер и жнец
    в core стали ranged; поле есть в редакторе;
  - **сепарация** — стая при преследовании расходится, а не строится в колонну.
- **Уровни сложности** (Лёгкая/Обычная/Тяжёлая): множитель силы врагов
  ×0.75/×1.0/×1.35 (масштабирует и XP за убийство — риск вознаграждается),
  переключение в настройках главного меню, хранится в user://settings.json.
- **Генерация данжей — в данные** (`dungeon.json` пресета): темы (текстуры/свет),
  пулы врагов по глубине, босс со свитой и наградой, множитель сложности,
  пул оружейного тайника. Нет файла → встроенные core-настройки; пустые
  themes/pools подменяются с предупреждением. Категории редактора «Данж: …».
- **Лут — в данные** (`loot.json`): предметы комнат данжа, точки патронов,
  дроп с убитых врагов (кумулятивная таблица одним броском). Категории «Лут: …».
- Дроп с убийств теперь **сидируется от сида данжа** — забег полностью
  воспроизводим (раньше сессионный RNG стартовал с константы).
- Тесты пресетов проверяют dungeon.json/loot.json: ссылки на врагов/предметы/
  оружие, существование текстур тем на диске, сумму шансов дропа ≤ 1.0;
  редактор при сохранении проверяет основные ссылки (пулы/босс/предметы лута).
  При первом редактировании данжа/лута в пресете без этих файлов редактор
  стартует от полной core-копии — усечённый файл не «выключит» дроп молча.
- **Диалоги data-driven** — `dialogues.json` пресета: реплики, ветвления (`next`),
  требования по статам, 9 видов эффектов (статы/отношения/флаги/золото/XP/квесты/флэш).
  JSON-сцены **приоритетнее story.rs**: переопределяют встроенные по id и добавляют
  новые без пересборки. Демо-сцены в core; формат — docs/DATA_FORMATS.md.
- **Редактор v2**:
  - категории «Диалоги» и «Пресет» (манифест); карт-категории пополнились
    флэтами, глоу-каналами, спавнами патронов и оружия;
  - **выбор файла карты** (`maps/*.json`) вместо захардкоженного hub.json;
  - типизированные поля: векторы спинбоксами, **цвет через палитру**;
  - **динамические выпадающие списки из данных пресета**: гивер ← NPC, цель
    квеста ← враги/предметы, вид спавна ← враги/предметы/оружие, сцена NPC ←
    диалоги, спрайт врага ← скан `enemy_*.png`;
  - 🔎 поиск по записям; пресеты из `user://presets`; **проверка ссылок при
    сохранении** (не блокирует, перечисляет проблемы);
  - отсутствующие секции карты создаются при добавлении записи (раньше
    «Добавить» в отсутствующую секцию молча терял запись).
- **ИИ-генерация текстур** — первый срез направления «нейросеть → ассеты»:
  - `tools/aigen.py` — клиент к серверу генерации (AUTOMATIC1111 `/sdapi/v1/txt2img`
    или ComfyUI `/prompt`), конфиг `tools/aigen.json` (url/бэкенд/модель/шаги);
  - шаблоны промптов по типам ассетов `tools/aigen_templates.json` (из ASSET_GUIDE);
  - автоматическая постобработка через `tools/process_sprites.py`: удаление фона,
    ресайз, сборка кадров, раскладка в `godot/assets/*` (включая маршрутизацию
    `dtile_*`/`sky_*` по подпапкам) — игра подхватывает PNG при следующем запуске;
  - окно **«🎨 ИИ-текстуры»** в редакторе OpenHeart: тип ассета + id + описание →
    кнопка «Сгенерировать», превью результата, генерация в фоне (редактор не виснет).
- **Пауза (Esc)**: меню паузы в Explore-режиме (продолжить / выйти в главное меню);
  бой останавливается в ЛЮБОМ меню — в инвентаре, перках и диалогах враги
  заморожены, снаряды замирают и урон по игроку не проходит (раньше враги били
  игрока, пока открыт инвентарь).
- `cargo test`: тесты контента — все JSON каждого пресета парсятся и проверяются
  на ссылочную целостность (квест → NPC/враг/предмет, спавны карт → enemies/items);
  встроенные фолбэк-копии тоже проверяются.

### Fixed
- **Враги рендерились одинаково** (белый спрайт грунта вместо своих листов/цветов/
  масштаба): `configure()` выполнялся после `add_child`, а `ready()` строит спрайт
  синхронно внутри `add_child` из ещё дефолтных полей. Конфигурация перенесена
  до `add_child` — теперь каждый вид врага показывает свой спрайт и цвет, и тинт
  элиты виден сразу при спавне (подтверждено headless-логом загрузки текстур).
  Латентный баг с первого коммита, найден при ревью аффиксов.

### Changed
- **DLL перенесена внутрь проекта**: `OpenHeart.gdextension` теперь указывает на
  `res://bin/openheart.dll` (раньше — `res://../rust/target/...`, вне проекта, из-за
  чего экспорт игры был невозможен). `build.bat`/`run.ps1`/`watch.ps1` копируют DLL
  в `godot/bin/` после сборки; папка в `.gitignore`.
- Битые `enemies.json`/`items.json`/`npcs.json`/`quests.json`/`level.json` больше не
  превращаются молча в пустые списки: предупреждение в лог + фолбэк на встроенную
  копию core (как у weapons/classes/perks). Неизвестный id в спавнах тоже логируется.
- Нечитаемый сейв больше не выглядит как «сейва нет»: файл откладывается в
  `user://save.corrupt.json` с предупреждением; версия сейва проверяется при чтении.
- `Esc` больше не «отпускает мышь» — вместо этого открывает меню паузы
  (в инвентаре/перках `Esc` закрывает экран).

### Планируется
- Статусы урона (горение/кровотечение/стан), крит-статы, классовые активки (`F`).
- Звук и музыка (M7), рывок/слайд (M5), 3D-гизмо карт в редакторе.
- Разбиение `game.rs` на подсистемы, workspace `oh_core`/`oh_game`, тесты.
- Полный план: [docs/DESIGN_PLAN.md](docs/DESIGN_PLAN.md).

## [0.4.0] — 2026-07-04

Крупный релиз: пресеты, редактор игры, карты из JSON, вертикальные данжи.

### Added
- **Система пресетов** `godot/presets/<id>/` — каждый пресет = отдельная игра
  (свои оружие/классы/перки/враги/предметы/NPC/квесты/карты + `preset.json`).
  Переключение в главном меню; сейв помнит свой пресет. В комплекте: `core`
  («Неоновое Сердце») и `arena` («Кровавая арена»).
- **Редактор игры** — плагин `godot/addons/oh_editor`, main-screen вкладка «OpenHeart»:
  формы по схемам для всех типов контента, CRUD, сохранение пресета, создание
  пресета-копии, «Замок ядра» (read-only на фундаментальные файлы).
- **Карты из JSON** (`maps/*.json`, `rust/src/map.rs`): фигуры box/ramp/stairs/cylinder,
  здания с неон-вывесками, пропсы, свет, светящиеся неон-каналы, спавны, врата данжа,
  окружение на карту (небо/туман/амбиент). Новый многоярусный хаб; карта арены.
- **Данжи 2.0**: комнаты на разных высотах (0/0.8/1.6), переменные потолки, ступени
  на границах уровней, платформы; **миникарта данжа** (floor_map → текстура + точка игрока).
- **NPC и квесты data-driven** (`npcs.json`, `quests.json`): гиверы с автогенерируемыми
  диалогами (взять/прогресс/сдать) и цепочками; трекинг kill/collect/clear_dungeon.
- Контент-пак: **14 типов врагов** (варианты по спрайту/масштабу с тематическими
  резистами), 18 предметов, 18 перков, 8 синергий, 12 NPC, 7 квестов.
- Постобработка (виньетка + хроматическая аберрация + сканлайны), glow/bloom,
  ACES-тонмаппинг, HUD-иконки HP/боезапаса.
- Поля `sprite`/`scale` у врагов; `Effect::Xp` в диалогах.

### Changed
- Рендерер → **Forward Mobile (Vulkan)**; анизотропная фильтрация текстур,
  аттенюация света.
- Данные переехали из `godot/data/` в `godot/presets/core/`; загрузчики контента
  стали перезагружаемыми (смена пресета без перезапуска процесса).
- Спавны мира берутся из карты пресета (`level.json` остаётся фолбэком).

### Fixed
- Пустой `npcs.json` больше не подтягивает legacy-NPC (различаются «файла нет» и
  «пустой список»).
- Спавны арены отодвинуты от точки появления игрока (мгновенная смерть на старте).
- Имя локации в HUD берётся из карты, а не из захардкоженной строки.

## [0.3.0] — 2026-07-04

### Added
- **Дерево перков** (3 ветки, ранги, требования) и **синергии** (комбо-эффекты);
  очки за уровни; экран прокачки (`P`); персист в сейве.
- **Типы урона** (физический/огонь/энергия/пустота) и **резисты врагов** — выбор
  оружия обретает тактический смысл.
- Data-driven оружие и классы (`weapons.json`, `classes.json`) с фолбэком на
  встроенные копии при битом JSON.
- Смерть → возврат в хаб с потерей 25 % золота (вместо стирания сейва).

### Changed
- UI масштабируется под любое окно (`canvas_items`-стретч, база 1920×1080) —
  починены клики меню на не-1920 экранах.

## [0.2.0] — 2026-07-03

### Added
- Переход к 3D от первого лица: открытый мир (хаб + пустоши), процедурный данж
  с боссом и порталами, 3 класса × 3 спека, 8 стволов с FP-спрайтами,
  hitscan/мили/снаряды, XP/уровни, HUD, сейвы v2.
- Пайплайн нарезки ассетов из ChatGPT-атласов (`tools/slice_*.py`):
  оружие, пикапы, эффекты, UI, небо, 77 тайлов данжа, 35 пропсов.

## [0.1.0] — до 2026-07

### Added
- Прототип: визуальная новелла / top-down RPG на Redot + Rust (gdext):
  диалоги с выборами и статами, квесты, инвентарь, NPC, сейвы.
  Наследие этого этапа (`story.rs`, статы INT/CHR/…) живо и используется диалогами.
